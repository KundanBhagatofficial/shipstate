import { readState } from './store.js';import { chooseNext,readiness } from './core.js';import { isGitRepo,isClean,head,branch } from './git.js';import { detectSandbox } from './sandbox.js';
export function projectStatus(root=process.cwd()){const s=readState(root);const counts={};for(const t of s.tasks)counts[t.state]=(counts[t.state]||0)+1;return {project:s.project,profile:s.profile,counts,readiness:readiness(s.tasks),next:chooseNext(s.tasks),git:isGitRepo(root)?{clean:isClean(root),head:head(root),branch:branch(root)}:{repo:false},sandbox:detectSandbox(),tasks:s.tasks,runs:s.runs,evidence:s.evidence,decisions:s.decisions,metrics:s.metrics,locks:s.locks,plans:s.plans};}
