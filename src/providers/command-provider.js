import fs from 'node:fs';
import path from 'node:path';
import { spawnSync } from 'node:child_process';
import { providerDataDir } from './registry.js';

export function commandProviderConfig(id,root=process.cwd()){const file=path.join(providerDataDir(root,id),'config.json');try{return JSON.parse(fs.readFileSync(file,'utf8'));}catch{return null;}}
export function writeCommandProviderConfig(id,config,root=process.cwd()){const file=path.join(providerDataDir(root,id),'config.json');fs.writeFileSync(file,JSON.stringify(config,null,2)+'\n');return file;}
function envFor(cfg){const keep=['PATH','HOME','USER','LOGNAME','SHELL','TMPDIR','TEMP','TMP','SystemRoot','COMSPEC','PATHEXT','LANG','LC_ALL'];const env={SHIPSTATE_PROVIDER_SANDBOX:'1'};for(const k of keep)if(process.env[k])env[k]=process.env[k];for(const k of cfg?.envAllow||[])if(process.env[k])env[k]=process.env[k];return env;}
export function invokeCommandProvider(id,event,payload,root=process.cwd(),{optional=false,config=null}={}){const cfg=config||commandProviderConfig(id,root);if(!cfg?.command){if(optional)return null;throw new Error(`${id} provider is not configured with a command`);}const shell=process.platform==='win32'?'cmd':'sh',args=process.platform==='win32'?['/d','/s','/c',cfg.command]:['-lc',cfg.command],timeout=Math.max(1000,Math.min(Number(cfg.timeoutMs||60000),5*60*1000)),r=spawnSync(shell,args,{cwd:root,encoding:'utf8',input:JSON.stringify({event,payload}),env:{...envFor(cfg),SHIPSTATE_PROVIDER:id,SHIPSTATE_PROVIDER_EVENT:event},timeout,maxBuffer:4*1024*1024});if(r.error?.code==='ETIMEDOUT'){if(optional)return {error:`${id} provider timed out`};throw new Error(`${id} provider timed out after ${timeout}ms`);}if(r.status!==0){if(optional)return {error:(r.stderr||r.stdout||'provider command failed').trim().slice(0,4000)};throw new Error((r.stderr||r.stdout||'provider command failed').trim().slice(0,4000));}if(Buffer.byteLength(r.stdout||'')>2*1024*1024)throw new Error(`${id} provider output exceeds 2 MiB`);try{return JSON.parse(r.stdout||'null');}catch{if(optional)return {error:`${id} returned invalid JSON`};throw new Error(`${id} returned invalid JSON`);}}
