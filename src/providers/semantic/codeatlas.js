import { readIntegration,callIntegration } from '../../integrations.js';
export const codeAtlasProvider={id:'codeatlas',capabilities:['semantic-context'],network:false,readOnly:true,priority:80,detect(root){const cfg=readIntegration('codeAtlas',root);return {available:Boolean(cfg?.command),configured:Boolean(cfg?.command)};},score(){return 80;},query(input,root){return callIntegration('codeAtlas',input.action||'context',input,root);}};
