const UI_EXT=/\.(?:tsx|jsx|vue|svelte|astro|swift|dart|kt|kts|css|scss|sass|less)$/i;
const UI_WORDS=/\b(?:ui|ux|screen|page|view|component|layout|modal|dialog|form|button|navigation|responsive|accessibility|a11y|swiftui|compose|flutter|react native)\b/i;
export function isUiTask(task={},profile={}){if((task.files||[]).some(f=>UI_EXT.test(f)))return true;const text=`${task.title||''} ${task.objective||''} ${(task.acceptanceCriteria||[]).join(' ')}`;if(UI_WORDS.test(text))return true;return (profile.frameworks||[]).some(f=>/react|next|vue|svelte|angular|swiftui|flutter|react native|compose/i.test(String(f)))&&/front|client|mobile|web/i.test(text);}
export function structuralPolicy(task,profile,index){const repositoryFiles=Object.keys(index?.files||{}).length;const languages=(profile?.languages||[]).map(String),frameworks=(profile?.frameworks||[]).map(String);const mobile=languages.some(x=>/swift|dart|kotlin/i.test(x))||frameworks.some(x=>/flutter|react native|swift|compose/i.test(x));const highImpact=(task.risk||'medium')==='high'||(task.allowedPaths||[]).some(x=>x==='**'||x.includes('src/**'));const trivial=(task.files||[]).length===1&&/copy|text|typo|css|style/i.test(`${task.title||''} ${task.objective||''}`);return {repositoryFiles,languages,frameworks,platforms:mobile?['mobile']:[],mobile,highImpact,trivial};}
export function stackForProfile(profile={}){const f=(profile.frameworks||[]).map(x=>String(x).toLowerCase());for(const [needle,stack] of [['next','nextjs'],['react native','react-native'],['react','react'],['vue','vue'],['svelte','svelte'],['angular','angular'],['flutter','flutter'],['swift','swiftui'],['compose','jetpack-compose']])if(f.some(x=>x.includes(needle)))return stack;const langs=(profile.languages||[]).map(x=>String(x).toLowerCase());if(langs.includes('swift'))return 'swiftui';if(langs.includes('dart'))return 'flutter';if(langs.includes('kotlin'))return 'jetpack-compose';return null;}
