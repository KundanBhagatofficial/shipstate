import fs from 'node:fs';import path from 'node:path';import { spawnSync } from 'node:child_process';
export function integrationConfig(root=process.cwd()){const dir=path.join(root,'.shipstate','integrations');fs.mkdirSync(dir,{recursive:true});return {dir,codeAtlas:path.join(dir,'codeatlas.json'),gameForge:path.join(dir,'gameforge.json')};}
export function writeIntegration(name,config,root=process.cwd()){const p=integrationConfig(root);const target=p[name];if(!target)throw new Error(`Unknown integration: ${name}`);fs.writeFileSync(target,JSON.stringify(config,null,2)+'\n');return target;}
export function readIntegration(name,root=process.cwd()){const p=integrationConfig(root)[name];try{return JSON.parse(fs.readFileSync(p,'utf8'));}catch{return null;}}
export function callIntegration(name,event,payload,root=process.cwd(),{optional=false}={}){const cfg=readIntegration(name,root);if(!cfg?.command){if(optional)return null;throw new Error(`${name} integration is not configured with a command`);}const shell=process.platform==='win32'?'cmd':'sh';const args=process.platform==='win32'?['/d','/s','/c',cfg.command]:['-lc',cfg.command];const r=spawnSync(shell,args,{cwd:root,encoding:'utf8',input:JSON.stringify({event,payload}),env:{...process.env,SHIPSTATE_INTEGRATION:name,SHIPSTATE_EVENT:event}});if(r.status!==0){if(optional)return {error:(r.stderr||r.stdout||'integration failed').trim()};throw new Error((r.stderr||r.stdout||'integration failed').trim());}try{return JSON.parse(r.stdout||'null');}catch{throw new Error(`${name} integration returned invalid JSON`);}}
