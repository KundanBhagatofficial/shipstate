import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import { tempRepo,writeTask,sh } from './helpers.js';import { initStore,addTasks } from '../src/store.js';import { loadTaskFiles } from '../src/spec.js';import { runTask } from '../src/runner.js';import { verifyTask } from '../src/verifier.js';import { acceptTask } from '../src/actions.js';
test('acceptance refuses stale base',async()=>{const root=tempRepo('stale');initStore(root);const taskFile=writeTask(root);addTasks(loadTaskFiles(taskFile),root);sh(root,'git',['add','.gitignore','specs']);sh(root,'git',['commit','-m','shipstate control metadata']);const run=await runTask('TASK-001','manual',root);fs.writeFileSync(path.join(run.worktreePath,'src/app.js'),'export const greeting = () => "hello shipstate";\n');assert.equal(verifyTask('TASK-001',root).passed,true);fs.writeFileSync(path.join(root,'README.md'),'moved\n');sh(root,'git',['add','README.md']);sh(root,'git',['commit','-m','move head']);assert.throws(()=>acceptTask('TASK-001',root),/HEAD moved/);});
