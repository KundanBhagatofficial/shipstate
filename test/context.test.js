import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import { tempRepo,writeTask } from './helpers.js';import { initStore,addTasks,readState } from '../src/store.js';import { loadTaskFiles } from '../src/spec.js';import { compileContext,buildRepositoryIndex } from '../src/context.js';
test('context index resolves imports, symbols and records token metrics',()=>{const root=tempRepo('context');fs.writeFileSync(path.join(root,'src','helper.js'),'export function helper(){ return 1 }\n');fs.writeFileSync(path.join(root,'src','app.js'),'import { helper } from "./helper.js"; export const greeting=()=>helper();\n');initStore(root);addTasks(loadTaskFiles(writeTask(root)),root);const idx=buildRepositoryIndex(root);assert.ok(idx.files['src/app.js'].imports.includes('src/helper.js'));assert.ok(idx.symbols.helper.includes('src/helper.js'));const c=compileContext('TASK-001',root,{budgetTokens:4000});assert.ok(c.tokens>0);assert.ok(c.files.includes('src/app.js'));assert.equal(readState(root).metrics.contexts.length,1);});
