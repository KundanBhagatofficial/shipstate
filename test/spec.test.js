import test from 'node:test';import assert from 'node:assert/strict';import { parseTaskMarkdown } from '../src/spec.js';
test('task contract parses execution policy',()=>{const t=parseTaskMarkdown(`# X\nId: TASK-X\nDepends On: A, B\nRisk: high\n## Objective\nDo X\n## Verification\n- npm test\n## Allowed Paths\n- src/**\n## Evidence\n- test`);assert.deepEqual(t.dependsOn,['A','B']);assert.equal(t.risk,'high');assert.deepEqual(t.allowedPaths,['src/**']);assert.deepEqual(t.evidenceRequired,['test']);});
