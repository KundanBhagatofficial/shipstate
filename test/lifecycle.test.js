import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import { tempRepo,writeTask,sh } from './helpers.js';import { initStore,addTasks,readState } from '../src/store.js';import { loadTaskFiles } from '../src/spec.js';import { runTask } from '../src/runner.js';import { verifyTask } from '../src/verifier.js';import { acceptTask } from '../src/actions.js';
test('manual isolated lifecycle verifies evidence then explicitly integrates',async()=>{const root=tempRepo('life');initStore(root);const taskFile=writeTask(root);addTasks(loadTaskFiles(taskFile),root);sh(root,'git',['add','.gitignore','specs']);sh(root,'git',['commit','-m','shipstate control metadata']);const run=await runTask('TASK-001','manual',root);assert.equal(fs.readFileSync(path.join(root,'src/app.js'),'utf8').includes('hello shipstate'),false);fs.writeFileSync(path.join(run.worktreePath,'src/app.js'),'export const greeting = () => "hello shipstate";\n');const v=verifyTask('TASK-001',root);assert.equal(v.passed,true);assert.equal(readState(root).tasks[0].state,'VERIFIED');assert.equal(fs.readFileSync(path.join(root,'src/app.js'),'utf8').includes('hello shipstate'),false);const a=acceptTask('TASK-001',root);assert.equal(a.accepted,true);assert.equal(readState(root).tasks[0].state,'ACCEPTED');assert.equal(fs.readFileSync(path.join(root,'src/app.js'),'utf8').includes('hello shipstate'),true);});
