import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import { tempRepo } from './helpers.js';import { initStore,verifyJournal,readState,paths } from '../src/store.js';
test('journal is checksum chained and legacy journals are upgraded',()=>{const root=tempRepo('journal');initStore(root);assert.equal(verifyJournal(root).valid,true);const p=paths(root);fs.writeFileSync(p.events,JSON.stringify({id:'old',type:'OLD',at:new Date().toISOString(),payload:{x:1}})+'\n');readState(root);const j=verifyJournal(root);assert.equal(j.valid,true);assert.ok(j.count>=1);});
